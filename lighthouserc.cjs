/**
 * .cjs, а не .json — ровно затем, чтобы у порога LCP ниже был настоящий
 * комментарий рядом с числом, а не в отдельном файле. Проект — ESM
 * ("type": "module" в package.json), а @lhci/utils грузит конфиг через
 * require() (node_modules/@lhci/utils/src/lighthouserc.js), поэтому
 * расширение обязано быть .cjs, а не .js — иначе require() в ESM-пакете
 * упадёт на несовпадении типа модуля.
 */
module.exports = {
  ci: {
    collect: {
      staticDistDir: './dist',
      // Явный URL вместо автообнаружения: без него staticDistDir находит
      // и index.html, и 404.html в dist/ и гоняет Lighthouse по обоим —
      // 404 закономерно проваливает categories.seo (у неё noindex) и не
      // имеет отношения к бюджету посадочной.
      url: ['/'],
      numberOfRuns: 3,
      // Без "preset" — это и есть мобильный профиль по умолчанию
      // (симулированные rtt 150мс, throughput 1638Kbps, cpuSlowdownMultiplier
      // 4х). У Lighthouse нет пресета "mobile" — desktop-профиль из
      // старого брифа противоречил бюджету, заданному именно для
      // мобильного (см. task-15-report.md).
    },
    assert: {
      assertions: {
        'categories:performance': ['error', { minScore: 0.98 }],
        'categories:seo': ['error', { minScore: 1 }],
        'categories:best-practices': ['error', { minScore: 1 }],
        'categories:accessibility': ['error', { minScore: 0.95 }],
        'cumulative-layout-shift': ['error', { maxNumericValue: 0.02 }],
        /**
         * Порог 1200мс из исходного брифа задачи 15 признан постановщиком
         * нереалистичным для мобильного профиля Lighthouse: симуляция
         * добавляет фиксированный пол ~615мс на новое соединение (rtt
         * 150мс × 3 круговых пробега на DNS+TCP+TLS+запрос — проверено
         * отдельно на пустой странице без CSS/фото/шрифтов, см.
         * task-15-report.md) ещё до единого байта контента. Официальный
         * порог самого Google для оценки LCP "хорошо" — 2500мс.
         *
         * Число ниже — не переподгонка под текущий результат "лишь бы
         * прошло": сначала (раунд правок 1) были проверены и исправлены
         * два реальных дефекта:
         *   - предзагрузка латинского файла шрифта наравне с кириллицей,
         *     хотя LCP-элемент (H1) целиком кириллический — конкуренция
         *     за канал на симулированном 4G откладывала отрисовку
         *     заголовка (исправлено в Base.astro, снят preload латиницы);
         *   - гипотеза про анимацию появления (.reveal с opacity:0 в
         *     ключевых кадрах) проверена и ОПРОВЕРГНУТА измерением:
         *     удаление класса reveal с текстового блока Hero не изменило
         *     LCP (1806-1829мс что с классом, что без), а прямая проверка
         *     в браузере показала opacity:1 и пустой getAnimations() уже
         *     на первом кадре — анимация не создаёт активный Animation-
         *     объект для элемента, который был в кадре уже при загрузке
         *     (animation-range уже пройден на старте). Класс не снят —
         *     снимать было бы нечего чинить.
         *
         * После обоих экспериментов фактический LCP на чистой сборке —
         * 1730-1753мс на 9 прогонах (3 раунда по 3). Худшее измеренное
         * значение (1753) + запас 14% (внутри вилки 10-15%, заданной
         * постановщиком) ≈ 1998мс — совпадает с потолком в 2000мс
         * практически вплотную, поэтому порог ниже — ровно 2000, а не
         * что-то более консервативное: и вилка запаса, и жёсткий потолок
         * упираются в одно и то же число.
         */
        'largest-contentful-paint': ['error', { maxNumericValue: 2000 }],
        'total-byte-weight': ['error', { maxNumericValue: 819200 }],
        'unused-css-rules': ['warn', { maxLength: 0 }],
      },
    },
  },
};
